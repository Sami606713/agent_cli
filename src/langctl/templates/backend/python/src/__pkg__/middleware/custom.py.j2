"""Your own middleware.

Add classes here with `langctl add middleware --custom <name>`, then list them
under `middleware.custom` in agent.yaml so they are wired into MIDDLEWARE.
"""

from __future__ import annotations
